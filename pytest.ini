[pytest]
# Default run is the pure, no-network unit suite. Integration tests hit external
# APIs / network (yfinance, FRED, Anthropic) and assert on drifting live data, so
# they are excluded by default. Run them explicitly with:  pytest -m integration
addopts = -m "not integration"
markers =
    integration: marks tests as integration tests that require external APIs or network access
